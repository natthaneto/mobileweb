import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cpkku.natthanet.lab07', // เปลี่ยน yourname เป็นชื่อย่อของคุณ
  appName: 'lab07-authen',
  webDir: 'dist'
};

export default config;
